export interface ProductDetail {
  name: string;
  category: string;
  brand?: string;
  size?: string;
  image: string;
  description: string;
  variant: string;
  option: string;
  price: number;
}
